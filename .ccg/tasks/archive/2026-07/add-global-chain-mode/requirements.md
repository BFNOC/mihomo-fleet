# 需求

新增一种实例运行模式：

- 现有模式继续保留，仍可使用订阅提供的分流规则。
- 新模式只使用订阅里的指定节点选择，不继承订阅分流。
- 新模式允许搭配用户本地添加的节点组成链式代理。
- 新模式本质上是全局代理：进入该实例端口的流量全部走代理链，不按订阅规则分流。

验收重点：

- 能在配置生成阶段产出 `GLOBAL` 或等效全局链式代理配置。
- 不破坏已有实例配置和订阅选择模式。
- 前端/API 能表达并持久化该模式。
- 测试覆盖配置生成或存储兼容性。
