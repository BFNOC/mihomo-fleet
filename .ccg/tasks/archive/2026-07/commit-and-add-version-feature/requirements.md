# 提交已有改动并添加版本号功能

## 需求

- 将已完成的实例代理多地址绑定、全局链式概览展示和日志滚动修复写成业务 commit。
- 添加程序版本号功能，使本地 `go run`、普通 `go build`、WebUI 顶部和 `-version` 使用同一版本解析。
- 版本真相源为根目录 `VERSION`。
- 不再调用多模型审查。

## 实现边界

- 不提交 `AGENTS.md`。
- CCG 归档与业务 commit 分离。
